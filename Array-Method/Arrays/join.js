/* - The join()Method joins all elemnsts of an array into a string, 
with an optional sperator - */

const colors = ['red', 'blue', "green"];
const colorString = colors.join('*');
console.log(colorString);  //  ( red*blue*green)

const fruits = ['apple', 'banana', 'kiwi'];
const bestfruits = fruits.join("naasir");
console.log(bestfruits); // apple naasir banana naasir kiwi


const colors1= ['red', 'blue', "green"];
const colorString1= colors.join('*');
console.log(colorString1);