// Reverse:- It reverses the order of the elment in an array in place

const letters = ['a', 'b', 'c'];
letters.reverse();
console.log(letters);  // c, b, a


const numbers = [1, 2, 3, 4 ];
numbers.reverse();
console.log(numbers);  // 4, 3,2 , 1



const letters1 = ['a', 'b', 'c'];
letters.reverse();
console.log(letters1)